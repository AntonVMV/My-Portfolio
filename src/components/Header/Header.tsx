import { NavBar } from "../NavBar/NavBar";
import { StyledHeader } from "./styles";

export const Header: React.FC = () => {
  return (
    <StyledHeader>
      <NavBar />
    </StyledHeader>
  );
};
